"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Utils;
(function (Utils) {
    function toPascalCase(text) {
        var camelText = toCamelCase(text);
        return camelText[0].toUpperCase() + camelText.slice(1);
    }
    Utils.toPascalCase = toPascalCase;
    function toCamelCase(text) {
        if (!text) {
            return '';
        }
        text = text.replace(text[0], text[0].toLowerCase());
        return text.replace(/([a-z])-([a-z])/ig, function (matches, g1, g2) { return "" + g1 + g2.toUpperCase(); });
    }
    Utils.toCamelCase = toCamelCase;
})(Utils = exports.Utils || (exports.Utils = {}));
//# sourceMappingURL=utils.js.map