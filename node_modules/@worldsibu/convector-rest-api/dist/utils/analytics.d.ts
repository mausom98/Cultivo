import * as Insight from 'insight';
export declare class Analytics {
    trackingCode: string;
    insight: Insight;
    constructor();
    init(): void;
    trackNewProject(label?: string): void;
    trackGenerateCC(label?: string): void;
    trackGenerateModel(label?: string): void;
    trackGenerateController(label?: string): void;
    track(category: CategoryEnum, action: ActionEnum, label?: string): void;
    permissionAcceptance(): void;
    permissionRejection(): void;
    returnAccepted(): void;
    returnRejected(): void;
}
export declare enum CategoryEnum {
    TRACKING = "tracking",
    PROJECT = "project",
    CHAINCODE = "chaincode",
    MODEL = "model",
    CONTROLLER = "controller"
}
export declare enum ActionEnum {
    NEW = "new",
    GENERATE = "generate"
}
