import { SmartModel } from '../models/smartModel';
export declare class SmartRoutesModels extends SmartModel {
    name: string;
    chaincodeName: string;
    projectName: string;
    ignoreConvention?: boolean;
    constructor(name: string, chaincodeName: string, projectName: string, ignoreConvention?: boolean);
    recompile(): void;
    save(): Promise<void>;
    readonly applicationName: string;
    readonly templateFile: string;
    readonly filePath: string;
}
