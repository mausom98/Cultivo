export declare abstract class SmartModel {
    name: string;
    projectName?: string;
    constructor(name: string, projectName?: string);
    readonly projectRoot: string;
    abstract save(): Promise<void | {}>;
}
